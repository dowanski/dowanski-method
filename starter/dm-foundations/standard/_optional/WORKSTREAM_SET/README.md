# Workstream — `{{WORKSTREAM_NAME}}`

Status: `{{PROPOSED_READY_ACTIVE_BLOCKED_IN_REVIEW_CLOSED}}`  
Parent: `../../CURRENT_STATE.md`  
Owner: `{{PERSON_OR_ROLE}}`

## Outcome

`{{INDEPENDENTLY_COORDINATED_RESULT_RETURNED_TO_THE_MAIN_SET}}`

## Why this is a workstream

Trigger:

- `{{INDEPENDENT_AUTHORITY_RELEASE_TRUST_RECOVERY_HANDOFF_CONCURRENCY_OR_THREE_SUPPORTING_SIGNALS}}`

This folder exists because the work needs an independent route and gate. If one
packet can carry the outcome without that boundary, use the direct Main Set
packet instead.

## Parent relationship

- Inherited purpose: `{{PROJECT_PURPOSE_REFERENCE}}`
- Inherited authority: `{{BOUNDARY}}`
- Narrowed authority: `{{LOCAL_LIMIT}}`
- Project behavior to preserve: `{{INVARIANT}}`

## Local reading path

1. this README;
2. `WORK_PACKET.md`;
3. only the optional contract, source, design, evidence, or runbook named by the packet.

## Current packet

- Path: `WORK_PACKET.md`
- Outcome: `{{PACKET_OUTCOME}}`
- Eligibility: `{{READY_CONDITION}}`

## Dependency and handoff

- Begins after: `{{DEPENDENCY_OR_NONE}}`
- Blocks: `{{DEPENDENT_WORK_OR_NONE}}`
- Safe pause state: `{{HOW_ANOTHER_PERSON_CAN_RESUME}}`

## Evidence and owner gate

- Evidence required: `{{EVIDENCE_SUMMARY}}`
- Review owner: `{{PERSON_OR_ROLE}}`
- Exit gate: `{{EXACT_ACCEPTANCE_CONDITION}}`

## Closure return

On closure, update the Main Set with:

- `{{CURRENT_CAPABILITY_OR_LIMITATION}}`;
- `{{DURABLE_DECISION_OR_NONE}}`;
- `{{SURVIVING_DEBT_OR_NONE}}`;
- `{{NEXT_LEGAL_ENTRY}}`.

## Closure sequence

1. Record the review disposition and matched evidence.
2. Return capability or limitation, durable decisions, surviving debt, and the
   next legal entry to their canonical Main Set records.
3. Mark the packet Closed and freeze it.
4. Remove the packet and workstream from active routers; preserve them through
   sufficient authorized version history or an earned archive.
5. Create and route a successor only when its entry conditions and authority
   are satisfied.

